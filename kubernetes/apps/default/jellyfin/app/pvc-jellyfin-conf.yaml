apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-jellyfin-conf
  namespace: default
spec:
  resources:
    requests:
      storage: 8000Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn