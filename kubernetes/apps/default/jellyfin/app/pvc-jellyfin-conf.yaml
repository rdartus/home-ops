apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-jellyfin-conf
  namespace: default
spec:
  resources:
    requests:
      storage: 8Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn