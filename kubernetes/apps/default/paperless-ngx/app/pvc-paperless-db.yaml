apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-paperless-db
  namespace: default
spec:
  resources:
    requests:
      storage: 200Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn