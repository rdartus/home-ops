apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-paperless-media
  namespace: default
spec:
  resources:
    requests:
      storage: 200Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn