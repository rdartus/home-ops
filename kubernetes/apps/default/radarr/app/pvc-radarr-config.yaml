apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-radarr-config
  namespace: default
spec:
  resources:
    requests:
      storage: 1536Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn