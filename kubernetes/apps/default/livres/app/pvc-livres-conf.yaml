apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-livres-conf
  namespace: default
spec:
  resources:
    requests:
      storage: 200Mi
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn