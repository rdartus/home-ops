apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-tandoor-static
  namespace: default
spec:
  resources:
    requests:
      storage: 500Mi
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn